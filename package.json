{
  "name": "juego-cartas",
  "version": "1.0.0",
  "description": "Multiplayer card game with Poker and Spanish decks",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:server": "node server/server.js",
    "dev:client": "cd client && vite",
    "build": "cd client && vite build",
    "start": "node server/server.js",
    "install": "cd client && npm install || true"
  },
  "keywords": ["card game", "multiplayer", "socket.io"],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {}
}
